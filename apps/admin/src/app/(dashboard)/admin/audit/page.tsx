"use client"
import {useEffect,useState} from "react"
import {RefreshCw,ShieldCheck} from "lucide-react"
import {Card,CardContent,CardDescription,CardHeader,CardTitle} from "@/components/ui/card"
import {Button} from "@/components/ui/button"
import {getAllAuditLogs} from "@/lib/firebase/services/admin-audit"
import {useAdminAuth} from "@/contexts/admin-auth-context"
export default function AuditPage(){const{hasPermission}=useAdminAuth();const[logs,setLogs]=useState<any[]>([]),[loading,setLoading]=useState(true),[error,setError]=useState("");const load=async()=>{setLoading(true);try{setLogs(await getAllAuditLogs(200))}catch(e:any){setError(e.message||"Could not load audit logs")}finally{setLoading(false)}};useEffect(()=>{load()},[]);if(!hasPermission("viewAuditLogs"))return <div className="px-4 lg:px-6"><Card><CardHeader><CardTitle>Audit log</CardTitle><CardDescription>You do not have permission to view audit logs.</CardDescription></CardHeader></Card></div>;return <div className="px-4 lg:px-6 space-y-6"><div className="flex items-start justify-between"><div><p className="text-xs uppercase tracking-[.18em] text-primary">Administration</p><h1 className="text-3xl font-semibold tracking-tight mt-1">Audit log</h1><p className="text-sm text-muted-foreground mt-2">Recent administrator actions and changes.</p></div><Button variant="outline" onClick={load}><RefreshCw className="size-4 mr-2"/>Refresh</Button></div>{error&&<div className="text-sm text-destructive">{error}</div>}<Card><CardHeader><CardTitle>Activity</CardTitle><CardDescription>{logs.length} recent entries</CardDescription></CardHeader><CardContent><div className="divide-y">{loading?<p className="py-8 text-sm text-muted-foreground text-center">Loading…</p>:logs.length===0?<p className="py-8 text-sm text-muted-foreground text-center">No audit entries yet.</p>:logs.map(l=><div key={l.id} className="py-3 flex items-start gap-3"><ShieldCheck className="size-4 mt-1 text-muted-foreground"/><div className="min-w-0 flex-1"><p className="text-sm"><span className="font-medium">{l.adminName||l.adminEmail||"Admin"}</span> · <span className="capitalize">{String(l.action||"").replaceAll("_"," ")}</span></p><p className="text-xs text-muted-foreground mt-1">{l.targetName||l.targetId||"—"}{l.details?` · ${l.details}`:""}</p></div><time className="text-xs text-muted-foreground whitespace-nowrap">{l.timestamp?new Date(l.timestamp).toLocaleString():"—"}</time></div>)}</div></CardContent></Card></div>}
