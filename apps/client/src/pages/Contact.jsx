import React,{useState} from "react"
import {motion} from "framer-motion"
import {Check,Loader2,MessageSquare} from "lucide-react"
import {useStore} from "../context/StoreContext"
import {api} from "../lib/api"
import PageTransition from "../components/PageTransition"
export default function Contact(){const{dark,user}=useStore();const[form,setForm]=useState({name:user?.name||"",email:user?.email||"",phone:user?.phone||"",type:"general",message:""}),[saving,setSaving]=useState(false),[done,setDone]=useState(false),[error,setError]=useState("");const submit=async e=>{e.preventDefault();setSaving(true);setError("");try{await api.sendContact(form);setDone(true);setForm(f=>({...f,message:""}))}catch(err){setError(err.message||"Could not send your message.")}finally{setSaving(false)}};return <PageTransition><main className="px-6 pt-12 pb-24"><div className="max-w-5xl mx-auto grid lg:grid-cols-[.8fr_1.2fr] gap-10"><div><p className="section-kicker">ARTCANVAS / CUSTOMER CARE</p><h1 className="font-display italic text-4xl font-bold mt-2">Talk to the studio.</h1><p className="mt-4 text-sm opacity-65 max-w-md">Questions, complaints, feedback or order concerns—send them here and the ArtCanvas team can follow up from the admin studio.</p><div className="mt-8 rounded-2xl border border-current/10 p-5"><MessageSquare size={18}/><p className="font-medium mt-3">What can you send?</p><ul className="mt-3 space-y-2 text-xs opacity-65"><li>• Order or delivery complaints</li><li>• Product feedback</li><li>• General questions</li><li>• Membership or payment questions</li></ul></div></div><form onSubmit={submit} className="rounded-2xl border border-current/10 p-6 md:p-8 space-y-5"><div className="grid sm:grid-cols-2 gap-4"><input required value={form.name} onChange={e=>setForm({...form,name:e.target.value})} placeholder="Your name" className="px-3 py-3 rounded-xl border border-current/10 bg-transparent text-sm"/><input required type="email" value={form.email} onChange={e=>setForm({...form,email:e.target.value})} placeholder="Email" className="px-3 py-3 rounded-xl border border-current/10 bg-transparent text-sm"/><input value={form.phone} onChange={e=>setForm({...form,phone:e.target.value})} placeholder="Phone (optional)" className="px-3 py-3 rounded-xl border border-current/10 bg-transparent text-sm"/><select value={form.type} onChange={e=>setForm({...form,type:e.target.value})} className="px-3 py-3 rounded-xl border border-current/10 bg-transparent text-sm"><option value="general">General enquiry</option><option value="complaint">Complaint</option><option value="feedback">Feedback</option></select></div><textarea required rows={7} value={form.message} onChange={e=>setForm({...form,message:e.target.value})} placeholder="Write your message…" className="w-full px-3 py-3 rounded-xl border border-current/10 bg-transparent text-sm"/><button disabled={saving} className="w-full rounded-full bg-black text-white dark:bg-white dark:text-black py-3 text-xs font-semibold uppercase tracking-wider flex items-center justify-center gap-2">{saving?<Loader2 className="animate-spin" size={15}/>:done?<Check size={15}/>:null}{done?"Sent successfully":"Send to studio"}</button>{error&&<p className="text-xs text-red-600">{error}</p>}</form></div></main></PageTransition>}